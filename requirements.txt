# VLSP 2025 Knowledge Distillation Pipeline - Requirements
# ============================================================
# Pinned versions tested with:
#   - Kaggle RTX 6000 Pro 96GB (Python 3.10, CUDA 12.x)
#   - Kaggle P100 16GB (Python 3.10, CUDA 12.x)
# ============================================================

# Core
pandas>=2.0.0,<3.0
tqdm>=4.60.0
pyarrow>=14.0.0
pyyaml>=6.0
gdown>=5.0.0

# ML Core
torch>=2.1.0
transformers>=4.46.0
peft>=0.13.0
accelerate>=0.34.0
datasets>=2.20.0
bitsandbytes>=0.44.0
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=4.0

# Tokenization
tokenizers>=0.20.0
huggingface_hub>=0.25.0

# Optional: Flash Attention 2 (RTX 6000 Pro, A100, H100 only - NOT P100)
# pip install flash-attn --no-build-isolation

# Optional: vLLM for fast serving
# pip install vllm>=0.6.0

# Optional: TRL for GRPO training
# pip install trl>=0.12.0

# Optional: API inference
# pip install openai>=1.0.0

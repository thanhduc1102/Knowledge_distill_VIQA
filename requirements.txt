# VLSP 2025 Knowledge Distillation Pipeline - Requirements
# ============================================================
# Tested with:
#   - Kaggle RTX 6000 Pro 96GB (Python 3.12, CUDA 12.x)
#   - Kaggle P100 16GB (Python 3.12, CUDA 12.x)
# ============================================================

# Core
pandas>=2.0.0
tqdm>=4.60.0
pyarrow>=14.0.0
pyyaml>=6.0

# ML Core
torch>=2.1.0
transformers>=5.0.0
peft>=0.18.0
accelerate>=1.0.0
datasets>=4.0.0
bitsandbytes>=0.44.0
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=4.0

# Tokenization
tokenizers>=0.20.0
huggingface_hub>=1.0.0

# Evaluation
sympy>=1.12

# RL Training
trl>=1.0.0

# Optional: Flash Attention 2 (RTX 6000 Pro, A100, H100 only - NOT P100)
# pip install flash-attn --no-build-isolation

# Optional: Kaggle upload
# pip install kagglehub
